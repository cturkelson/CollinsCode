package SuiteOfGames;

/**
 * TempPile creates a temporary stack to hold cards when moving more than
 * one at a time.
 * @author Christina Kidwell
 */
class TempPile extends CardPile {

    TempPile(final int x, final int y) {
        super(x, y);
    }

}